/**
 * Freighter wallet integration — scaffolding only.
 *
 * Feature logic for wallet connection, challenge signing, and Soroban RPC
 * calls will live in this module in a later phase. The
 * `@stellar/freighter-api` package is already a dependency of this project
 * to build on.
 */
export {};
