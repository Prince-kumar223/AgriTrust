# Environment template for the AgriTrust frontend.
# Copy to `.env` and adjust. `cp .env.example .env`
#
# Base URL of the Django REST API (used by the API client, added in a later phase).
VITE_API_BASE_URL=http://localhost:8000
